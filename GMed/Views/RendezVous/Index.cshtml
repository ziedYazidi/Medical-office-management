@model IEnumerable<GMed.Models.RendezVous>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ActeMedical.Traitement)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ActeMedicalId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DateRendezVous)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AdresseRendezVous)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ActeMedical.Traitement)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ActeMedicalId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DateRendezVous)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AdresseRendezVous)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.RendezVousId }) |
            @Html.ActionLink("Details", "Details", new { id=item.RendezVousId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.RendezVousId })
        </td>
    </tr>
}

</table>
